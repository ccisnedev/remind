# app

Presentation layer — views, controllers, client surfaces.

**Depends on:** `api`.
**Must not depend on:** `db` (reach data through `api`), `infra`.

**Canonical example:** _pending._ The first screen or controller added here
becomes the pattern the others replicate.

> A framework scaffolder run inside this folder will not overwrite this file.
> Scaffolders skip files that already exist, so this anchor is safe to keep.
